{
  "name": "n9-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "deploy": "bun run sync-packages && astro build && wrangler deploy",
    "build-deb": "bash scripts/build-deb.sh",
    "publish": "bun run scripts/publish-repo.ts",
    "sync-packages": "bun run scripts/sync-packages.ts"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "alpinejs": "^3.14.0",
    "astro": "^5.0.0",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
